# Short VN Demo Assets

The current demo uses runtime fallback textures generated in `src/main.rs`, so it can run without binary art assets.

Future demo assets must be original, CC0, or otherwise clearly licensed for redistribution. Do not add game-specific extracted assets or decryption outputs here.
