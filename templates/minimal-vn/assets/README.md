# Assets

Place project images, audio files, and generated manifests here.

The template runs without real assets because `src/main.rs` registers tiny fallback textures when this directory does not contain loadable textures.
