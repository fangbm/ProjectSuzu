# Project Suzu Continuation Context

Last updated: 2026-05-15 18:56:40 +08:00

This is the compressed handoff for continuing Project Suzu. Update this file at the end of each future Codex conversation on this repository before sending the final reply. Treat live `git status`, GitHub Releases, and GitHub Actions as the source of truth when they differ from this note.

## Project Identity

- Product name: `Project Suzu`.
- Repository: `fangbm/ProjectSuzu`.
- Local path: `D:\codex\projectSuzu`.
- Integration branch: `main`.
- Engineering convergence branch: `feature/v0.1.5-engineering-convergence`.
- Historical XP3 preview branch: `feature/xp3-archive-support`.
- License: `MIT OR Apache-2.0`.

## Current Repo State

- Current branch after this work: `main`.
- `v0.1.5` was tagged from the framework-guide commit `e749f9e Add detailed framework guide` and released successfully on 2026-05-15.
- `main` includes post-`v0.1.5` follow-up commit `5a7c237 Add getting started template and check diagnostics`.
- This conversation added XP3 viewer responsiveness work after `v0.1.5`: background XP3 index loading, background entry preview/decode, no automatic first-entry decrypt on archive load, and hidden Windows external processor windows. Do not retag `v0.1.5`; use a later tag for the next release.
- One old stash remains: `stash@{0}: On feature/xp3-archive-support: local xp3 legal cleanup before main merge`. It was retained from earlier cleanup work; do not drop it unless the user asks.

## v0.1.5 Work Completed

- Split `crates/suzu-app/src/app.rs` into focused runtime modules under `crates/suzu-app/src/app/`; `app.rs` is now about 290 lines.
- Kept public runtime exports stable: `SuzuApp`, `TitleMenuAction`, and `SystemMenuAction` remain exported as before.
- Split `suzu-launcher` and `suzu-xp3-viewer` into smaller app/UI/helper modules; their `main.rs` files are about 30 lines.
- Added headless GUI tool checks:
  - `suzu-launcher --check [--project-root <dir>] [--xp3 <file>] [--xp3-plugin <json> --i-have-rights-to-process-these-assets]`
  - `suzu-xp3-viewer --check [--xp3 <file>] [--xp3-plugin <json> --i-have-rights-to-process-these-assets]`
  - `suzu-editor --check [--project-root <dir>]`
- Added workspace member `tests/smoke` covering example script compilation, runtime progression, `.suzupack` package loading through `AssetManager`, save/restore, plaintext XP3 resource reading, and KAG-to-Suzu conversion.
- Added a minimal `suzu-packer` library entry and package archive registration/reading through `AssetManager`.
- Added XP3 plugin unsupported-format, placeholder, relative path, stderr, byte-count mismatch, and authorization tests.
- Added `THIRD_PARTY_LICENSES.md` generated by `cargo-about`, `about.toml`, `about-markdown.hbs`, `assets/branding/README.md`, and `docs/api-stability.md`.
- Strengthened `LEGAL.md` and `SECURITY.md` around external XP3 plugin risk.
- Updated release/package scripts to require and include third-party notices, branding notes, XP3 docs, API stability docs, legal, security files, and framework documentation.
- Bumped crate/tool/example/smoke package versions to `0.1.5` and updated `CHANGELOG.md`.
- Added `docs/xp3-plugin-interface.md` for `suzu.xp3-plugin.v1`, covering JSON module fields, external process stdin/stdout protocol, placeholders, stages, authorization, errors, synthetic identity processor examples, security guidance, and review checklist.
- Added `docs/framework-guide.md`, a detailed Chinese guide covering project setup, script writing, resources, runtime APIs, title screen, save/load, settings, desktop integration, tools, packaging, XP3 boundaries, validation, troubleshooting, and compatibility advice.

## 2026-05-15 Follow-Up Changes

- Published `v0.1.5` by pushing `main`, creating/pushing tag `v0.1.5`, and verifying the Release workflow.
- Verified Release workflow run `25909490542` completed successfully. Jobs passed: `Quality gate`, `Build ubuntu-latest`, `Build windows-latest`, and `Publish GitHub Release`.
- Verified GitHub Release `v0.1.5` exists: `https://github.com/fangbm/ProjectSuzu/releases/tag/v0.1.5`.
- Verified release assets and SHA-256 digests after downloading through local proxy `http://127.0.0.1:7890`:
  - `project-suzu-v0.1.5-linux-x64.tar.gz`, size `35966503`, SHA-256 `548a178e5a4c7b9a9516bbe6b191e85b16067d94e9b0e732c5ac270d20569eaa`.
  - `project-suzu-v0.1.5-windows-x64.tar.gz`, size `21844895`, SHA-256 `c30d641de934a0a8e1897aa84d60eb700201a42dd0187b9a6f1eba4c5e12d6c6`.
- Verified both release archives contain core binaries, README files, `CHANGELOG.md`, `LEGAL.md`, `SECURITY.md`, `CONTRIBUTING.md`, license files, `THIRD_PARTY_LICENSES.md`, branding notes/icon, `docs/framework-guide.md`, `docs/xp3-support.md`, `docs/xp3-plugin-interface.md`, `docs/api-stability.md`, and packed hello-world assets.
- Changed GUI `--check` success output from plain `check ok` to readable multi-line diagnostics:
  - `Project Suzu Launcher check OK`, version, features.
  - `Project Suzu XP3 Viewer check OK`, version, features.
  - `Project Suzu Editor check OK`, version, features.
- Changed GUI `--check` failures to print `Project Suzu ... check FAILED` plus `reason: ...`.
- Added launcher unit coverage for `suzu-launcher --check --xp3-plugin <json>` without the explicit authorization flag.
- Added `docs/getting-started.md` as the first-project walkthrough.
- Added `templates/minimal-vn` with `Cargo.toml`, `README.md`, `assets/README.md`, `script/main.szs`, and `src/main.rs`.
- Updated README files, `docs/user-guide.md`, `docs/framework-guide.md`, `docs/release-checklist.md`, `docs/release-packaging.md`, `scripts/package-desktop.ps1`, and `.github/workflows/release.yml` to point to/include the getting-started guide and minimal template in future packages.

## 2026-05-15 XP3 Preview Responsiveness Optimization

- Fixed severe UI stalls in `suzu-xp3-viewer` when loading or previewing encrypted/protected XP3 entries through an external plugin.
- `suzu-xp3-viewer` now loads the XP3 index on a background worker and keeps the egui UI repainting while the archive is loading.
- Loading an archive no longer automatically selects/decrypts/previews the first entry. The viewer waits for the user to select an entry, avoiding surprise external processor launches right after choosing a file.
- Entry preview now runs `archive.read_file` and preview data preparation on a background worker. The UI shows a loading state and creates the final egui texture only after decoded image/text data is ready.
- The viewer avoids starting overlapping preview workers from repeated entry clicks while a preview is still loading.
- On Windows, external XP3 processors launched by `suzu-asset` use `CREATE_NO_WINDOW`, which should stop the short console window flashes seen when the plugin command is `python.exe`.
- Tested against local user-provided paths:
  - XP3: `D:\Program Files\まいてつ Last Run!!\data.xp3`, size `821086680` bytes.
  - Plugin: `C:\Users\方便面\Documents\New project 3\maitetsu-xp3-plugin\xp3-plugin.json`.
  - The plugin command is `C:\ProgramData\miniconda3\python.exe` with stage `after_inflate`.
- Real-file `suzu-xp3-viewer --check --xp3 ... --xp3-plugin ... --i-have-rights-to-process-these-assets` passed.
- Real-folder `suzu-launcher --krkr-probe "D:\Program Files\まいてつ Last Run!!"` passed and reported 12 XP3 archives, with `data.xp3` containing 3220 entries, 817 script-like protected entries, and entrypoint candidates `main/default.tjs`, `scenario/start.ks`, `startup.tjs`.

## Verification Passed Locally

- `cargo fmt --all -- --check`
- `cargo fmt --manifest-path templates/minimal-vn/Cargo.toml -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo doc --workspace --no-deps`
- `cargo test -p suzu-script --features lua`
- `cargo test -p suzu-launcher`
- `cargo check --manifest-path templates/minimal-vn/Cargo.toml`
- `cargo run -p suzu-compiler -- templates\minimal-vn\script\main.szs`
- `cargo run -p suzu-launcher -- --check`
- `cargo run -p suzu-xp3-viewer -- --check`
- `cargo run -p suzu-editor -- --check`
- `.\scripts\package-desktop.ps1 -Check`
- `git diff --check`
- Latest XP3 preview optimization gate additionally passed:
  - `cargo test -p suzu-asset xp3_plugin -- --nocapture`
  - `cargo test -p suzu-xp3-viewer`
  - Real `data.xp3` plus local external plugin `suzu-xp3-viewer --check`.

## Release Status

Latest published release: `v0.1.5`.

- Release URL: `https://github.com/fangbm/ProjectSuzu/releases/tag/v0.1.5`.
- Release workflow run: `25909490542`, conclusion `success`.
- Release assets confirmed:
  - `project-suzu-v0.1.5-linux-x64.tar.gz`
  - `project-suzu-v0.1.5-windows-x64.tar.gz`

Previous stable release: `v0.1.4`, `https://github.com/fangbm/ProjectSuzu/releases/tag/v0.1.4`.

## User Preferences

- Keep the GitHub repository public.
- Use versioned release asset filenames.
- Release notes should show changes between versions.
- Keep a meaningful local commit before release automation.
- Verify release completion from Actions, artifacts, release existence, and release assets.
- Do not guess preview numbering; check GitHub Releases first.
- Prefer Windows and Linux assets unless the user asks for macOS again.
- If only workflow or documentation changes, do not bump the project version unless explicitly requested.
- For future network downloads, local proxy `http://127.0.0.1:7890` worked for GitHub release assets in this environment.

## XP3 And Legal Boundary

- Repository code supports plain, unencrypted XP3 archive reading.
- Repository code must not ship built-in XOR, ChaCha, PackinOne, game-specific decryptors, or decryptor examples.
- Keep only a neutral external XP3 plugin hook with explicit authorization confirmation.
- Do not upload future decryption plugins to the repository.
- Game-specific or decryption-related experiments are local only.
- Avoid public-facing claims of full KRKR compatibility.
- Preferred wording: XP3 archive reader, XP3-backed asset loading, XP3 asset preview, KRKR package scan mode.

## Commands

```powershell
$env:PATH = "$env:USERPROFILE\.cargo\bin;$env:PATH"
git status --short --branch
git log --oneline --decorate -10
gh release list --repo fangbm/ProjectSuzu --limit 20
gh run list --repo fangbm/ProjectSuzu --workflow Release --limit 5
gh release view v0.1.5 --repo fangbm/ProjectSuzu
cargo run --manifest-path templates\minimal-vn\Cargo.toml
```
